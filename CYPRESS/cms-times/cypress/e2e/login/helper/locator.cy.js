export class Locator {
	// input
	inputUsername = 'input[name="email"]';
	inputPassword = 'input[name="password"]';

	// button
	btnLogin = 'button[name="login"]';
	btnKeluar = 'a[href="/?page=logout"]';

    // icon
    navbarBrandCMS = '.navbar-brand';

    // alert
    alertError = '.alert';
	alertMinimum3CharUname = '.fv-plugins-message-container'
	alertMinimum3CharPass = '.fv-plugins-message-container'
}
